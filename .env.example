# Environment Configuration Example
# Copy this file to .env and set your environment

# Available environments:
# DEV = Bolt Playground (development)
# BETA = GitHub repository (testing) 
# PROD = Production version (future)
ENV=DEV

# Future database configuration (for PROD environment)
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key